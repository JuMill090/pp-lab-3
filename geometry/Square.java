package geometry;

public class Square extends Rectangle {
    public Square(double sideLength) {
        super(sideLength, sideLength); // Przekazujemy długość boku do konstruktora klasy bazowej Rectangle
    }
}